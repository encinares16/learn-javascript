
//  JS Array

let colors = ['blue', 'red', 'green']

// array methods
colors.push("orange")      // add an element
colors.pop()               // remove last element
colors.unshift("violet")   // add element to first
colors.shift()             // removes element to first

let length = colors.length
let index = colors.indexOf("blue")

console.log(length)
console.log(index)
